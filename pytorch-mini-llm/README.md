# PyTorch Mini LLM

本目录用于实现 Mini LLM 的 PyTorch 版本，计划与 `keras-mini-llm/` 保持对应的教学结构和主链路。

当前状态：准备迁移。

计划覆盖：

```text
BBPE tokenizer
  -> decoder-only Transformer pretraining
  -> LoRA-SFT
  -> LoRA-DPO
  -> prefill / decode model
  -> KVCache inference
```
